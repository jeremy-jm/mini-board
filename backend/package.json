{
  "name": "mini-board-backend",
  "version": "1.0.0",
  "description": "Fastify + TypeScript backend (minimal template)",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/server.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "keywords": [],
  "author": "Jeremy Zhou",
  "license": "MIT",
  "dependencies": {
    "fastify": "^5.8.4",
    "@fastify/sensible": "^6.0.0"
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2",
    "vitest": "^4.1.2"
  }
}
